# spring.application.name=AjaTourismCorporation

# #db configuration details

# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.datasource.url=jdbc:mysql://localhost:3306/aja_tourism

# spring.datasource.username=root

# spring.datasource.password=tiger

# spring.jpa.hibernate.ddl-auto=update

spring.application.name=AjaTourismCorporation

# Railway DB connection (from Render ENV variables)
spring.datasource.url=${DATABASE_URL}?sslmode=require
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Required on Render
server.port=${PORT:8080}

# Connection pool settings (recommended)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
